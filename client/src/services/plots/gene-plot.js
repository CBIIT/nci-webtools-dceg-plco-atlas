import { ManhattanPlot } from 'manhattan-plot';

/**
 *
 * @param {ManhattanPlot} manhattanPlot
 * @param {*} config
 */

function drawGenePlot(manhattanPlot, config) {
  const { genes, ctx, overlayCtx, hiddenCtx } = config;
}
